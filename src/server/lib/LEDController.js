module.exports = class LEDController {
  /**
  * @constructor
  *
  * @param { http.Server } http  the express http server
  * @param { object } config the loaded configuration
  */
  constructor(http, config) {
    console.log('TODO: LEDController', http, config);
  }
};
